# Backend specific dependencies
fastapi>=0.100.0
uvicorn[standard]>=0.20.0

# Note: Other dependencies are in the main requirements.txt
# Install with: pip install -r ../requirements.txt -r requirements.txt
